version: 1

routes:
  # ---------------------------
  # WITHDRAWALS
  # ---------------------------
  - id: metamask-ethereum-usdc-withdrawal
    kind: withdraw
    description: "$101 USDC to Ethereum Withdrawal with MetaMask"
    wallet_type: metamask
    wallet_alias: metamask-ethereum-usdc-withdrawal
    amount: "101"
    src_chain: dydx
    dst_chain: ethereum
    cadence_min: 30
    enabled: true

  - id: metamask-polygon-usdc-withdrawal
    kind: withdraw
    description: "$101 USDC to Polygon Withdrawal with MetaMask"
    wallet_type: metamask
    wallet_alias: metamask-polygon-usdc-withdrawal
    amount: "101"
    src_chain: dydx
    dst_chain: polygon
    cadence_min: 30
    enabled: true

  - id: metamask-arbitrum-usdc-withdrawal
    kind: withdraw
    description: "$101 USDC to Arbitrum Withdrawal with MetaMask"
    wallet_type: metamask
    wallet_alias: metamask-arbitrum-usdc-withdrawal
    amount: "101"
    src_chain: dydx
    dst_chain: arbitrum
    cadence_min: 30
    enabled: true

  - id: metamask-base-usdc-withdrawal
    kind: withdraw
    description: "$101 USDC to Base Withdrawal with MetaMask"
    wallet_type: metamask
    wallet_alias: metamask-base-usdc-withdrawal
    amount: "101"
    src_chain: dydx
    dst_chain: base
    cadence_min: 30
    enabled: true

  - id: phantom-solana-usdc-withdrawal
    kind: withdraw
    description: "$101 USDC to Solana Withdrawal with Phantom"
    wallet_type: phantom
    wallet_alias: phantom-solana-usdc-withdrawal
    amount: "101"
    src_chain: dydx
    dst_chain: solana
    cadence_min: 30
    enabled: true

  # ---------------------------
  # DEPOSITS – $10 REGULAR
  # ---------------------------
  - id: metamask-ethereum-usdc-deposit-regular
    kind: deposit
    description: "$10 USDC from Ethereum Regular Deposit with MetaMask"
    wallet_type: metamask
    wallet_alias: metamask-ethereum-usdc-deposit-regular
    route_kind: regular
    amount: "10"
    src_chain: ethereum
    dst_chain: dydx
    cadence_min: 30
    enabled: true

  - id: metamask-arbitrum-usdc-deposit-regular
    kind: deposit
    description: "$10 USDC from Arbitrum Regular Deposit with MetaMask"
    wallet_type: metamask
    wallet_alias: metamask-arbitrum-usdc-deposit-regular
    route_kind: regular
    amount: "10"
    src_chain: arbitrum
    dst_chain: dydx
    cadence_min: 30
    enabled: true

  - id: metamask-base-usdc-deposit-regular
    kind: deposit
    description: "$10 USDC from Base Regular Deposit with MetaMask"
    wallet_type: metamask
    wallet_alias: metamask-base-usdc-deposit-regular
    route_kind: regular
    amount: "10"
    src_chain: base
    dst_chain: dydx
    cadence_min: 30
    enabled: true

  - id: metamask-polygon-usdc-deposit-regular
    kind: deposit
    description: "$10 USDC from Polygon Regular Deposit with MetaMask"
    wallet_type: metamask
    wallet_alias: metamask-polygon-usdc-deposit-regular
    route_kind: regular
    amount: "10"
    src_chain: polygon
    dst_chain: dydx
    cadence_min: 30
    enabled: true

  - id: phantom-solana-usdc-deposit
    kind: deposit
    description: "$10 USDC from Solana Regular Deposit with Phantom"
    wallet_type: phantom
    wallet_alias: phantom-solana-usdc-deposit
    route_kind: regular
    amount: "10"
    src_chain: solana
    dst_chain: dydx
    cadence_min: 30
    enabled: true

  # ---------------------------
  # INSTANT DEPOSITS – $101
  # ---------------------------
  - id: metamask-ethereum-usdc-deposit-instant
    kind: deposit
    description: "$101 USDC from Ethereum Instant Deposit with MetaMask"
    wallet_type: metamask
    wallet_alias: metamask-ethereum-usdc-deposit-instant
    route_kind: instant
    amount: "101"
    src_chain: ethereum
    dst_chain: dydx
    cadence_min: 30
    enabled: true

  - id: metamask-arbitrum-usdc-deposit-instant
    kind: deposit
    description: "$101 USDC from Arbitrum Instant Deposit with MetaMask"
    wallet_type: metamask
    wallet_alias: metamask-arbitrum-usdc-deposit-instant
    route_kind: instant
    amount: "101"
    src_chain: arbitrum
    dst_chain: dydx
    cadence_min: 30
    enabled: true

  - id: metamask-base-usdc-deposit-instant
    kind: deposit
    description: "$101 USDC from Base Instant Deposit with MetaMask"
    wallet_type: metamask
    wallet_alias: metamask-base-usdc-deposit-instant
    route_kind: instant
    amount: "101"
    src_chain: base
    dst_chain: dydx
    cadence_min: 30
    enabled: true

  - id: metamask-polygon-usdc-deposit-instant
    kind: deposit
    description: "$101 USDC from Polygon Instant Deposit with MetaMask"
    wallet_type: metamask
    wallet_alias: metamask-polygon-usdc-deposit-instant
    route_kind: instant
    amount: "101"
    src_chain: polygon
    dst_chain: dydx
    cadence_min: 30
    enabled: true
